{
  "name": "scp-os-worker",
  "version": "1.0.0",
  "description": "SCP Foundation scraper deployed on Cloudflare Workers",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@flicknote/defuddle": "^1.0.1",
    "cheerio": "^1.2.0",
    "linkedom": "^0.18.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "@vitest/coverage-v8": "^4.1.2",
    "vitest": "^4.1.2",
    "wrangler": "^4.69.0"
  }
}
